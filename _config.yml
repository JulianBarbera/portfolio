# The Site Configuration

# Import the theme
theme: jekyll-theme-chirpy

lang: en

# Change to your timezone › https://kevinnovak.github.io/Time-Zone-Picker
timezone: America/Los_Angeles

# jekyll-seo-tag settings › https://github.com/jekyll/jekyll-seo-tag/blob/master/docs/usage.md
# ↓ --------------------------

title: Julia Barbera

tagline: meow

description: >- 
  Julia Barbera's portfolio, blog, and other things she finds interesting

url: "jbarbera.space"

github:
  username: JulianBarbera

social:
  name: Julia Barbera
  email: julia@fishcat.fish
  links:
    - https://github.com/JuliaBarbera
    - https://www.linkedin.com/in/julianbarbera

# Site Verification Settings
webmaster_verifications:
  google: # fill in your Google verification code

# ↑ --------------------------
# The end of `jekyll-seo-tag` settings

# Web Analytics Settings
analytics:
  goatcounter:
    id: jbarbera

# Page views settings
pageviews:
  provider: goatcounter

# the avatar on sidebar, support local or CORS resources
avatar: assets/img/irl/avatar.png

# boolean type, the global switch for TOC in posts.
toc: true

comments:
  provider: #utterances
  utterances:
    repo: # <gh-username>/<repo>
    issue_term: # < url | pathname | title | ...>

pwa:
  enabled: true
  cache:
    enabled: true
    deny_paths:
      # - "/example"  # URLs match `<SITE_URL>/example/*` will not be cached by the PWA

paginate: 10

# The base URL of your site
baseurl: ""

# ------------ The following options are not recommended to be modified ------------------

kramdown:
  footnote_backlink: "&#8617;&#xfe0e;"
  syntax_highlighter: rouge
  syntax_highlighter_opts: # Rouge Options › https://github.com/jneen/rouge#full-options
    css_class: highlight
    # default_lang: console
    span:
      line_numbers: false
    block:
      line_numbers: true
      start_line: 1

collections:
  tabs:
    output: true
    sort_by: order

defaults:
  - scope:
      path: "" # An empty string here means all files in the project
      type: posts
    values:
      layout: post
      comments: true # Enable comments in posts.
      toc: true # Display TOC column in posts.
      # DO NOT modify the following parameter unless you are confident enough
      # to update the code of all other post links in this project.
      permalink: /posts/:title/
  - scope:
      path: _drafts
    values:
      comments: false
  - scope:
      path: ""
      type: tabs # see `site.collections`
    values:
      layout: page
      permalink: /:title/

sass:
  style: compressed

compress_html:
  clippings: all
  comments: all
  endings: all
  profile: false
  blanklines: false
  ignore:
    envs: [development]

exclude:
  - "*.gem"
  - "*.gemspec"
  - docs
  - tools
  - README.md
  - LICENSE
  - purgecss.js
  - "*.config.js"
  - "package*.json"

jekyll-archives:
  enabled: [categories, tags]
  layouts:
    category: category
    tag: tag
  permalinks:
    tag: /tags/:name/
    category: /categories/:name/
